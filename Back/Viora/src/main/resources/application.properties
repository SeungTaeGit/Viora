spring.application.name=Viora

spring:
    datasource:
        url: jdbc:mysql://localhost:3306/viora?useSSL=false&serverTimezone=Asia/Seoul
        username: root
        password: yourpassword
    jpa:
        hibernate:
            ddl-auto: update  # ?? create / none
        show-sql: true
        properties:
            hibernate:
                format_sql: true
